
<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        コーディネート詳細
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-7">
        <div class="card">
          <div class="card-image">
            <figure class="image is-4by3">
              <%= attachment_image_tag @coordination, :image %>
            </figure>
          </div>
          <div class="card-content">
            <div class="content">
              <table class="table is-narrow">
                <tr>
                  <th>コーディネート詳細</th>
                </tr>
                <tr>
                  <td><%= simple_format @coordination.body %></td>
                </tr>
              </table>
              <% if @coordination.user.id == current_user.id %>
                <%= link_to "編集画面へ", edit_coordination_path(@coordination), class: "button is-success" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="column is-4">
        <article class="panel is-link">
          <p class="panel-heading">
            By <%= @coordination.user.username %>
          </p>
          <div class="panel-block">
            <p class="control">
              <%= @coordination.user.profile %>
            </p>
          </div>
          <%= link_to user_path(@coordination.user), class: "panel-block" do %>
            <span class="panel-icon">
              <i class="fas fa-user" aria-hidden="true"></i>
            </span>
            <%= @coordination.user.username %> さんのページへ
          <% end %>
        </article>
      </div>
    </div>
  </div>
</section>